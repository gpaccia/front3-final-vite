import React from 'react'

const UserCard = ({usuario}) => {
    return (
        <>
          <h2>Gracias {usuario.nombre}, te contactaremos cuando antes vía mail.</h2>
        </>
      )
    }

export default UserCard